name: women_security_app
description: A simple women security app

publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.2
  firebase_messaging: ^14.9.1
  http: ^1.1.0

flutter:
  uses-material-design: true
